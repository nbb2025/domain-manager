version: '3.8'

services:
  app:
    image: ghcr.io/nbb2025/domain-manager:latest
    ports:
      - "8080:8080"
    volumes:
      - db_data:/app/backend/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

volumes:
  db_data:
    driver: local
